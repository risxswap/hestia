# Codex 项目约束

本文件约束 Codex 在本仓库中的产品讨论、文档撰写和代码实现行为。

## 沟通与文档语言

- 默认使用中文与用户沟通。
- 产品设计、PRD、spec、实现计划、评审说明、提交说明中的长文本默认使用中文。
- 只有在接入第三方英文 API、引用英文标准、编写代码注释或用户明确要求时，才使用英文。
- 已存在的英文文档不需要无故重写；新增文档优先使用中文。

## 产品定位

- 本项目是一个个人 AI 形象顾问 Agent，首选载体是微信小程序。
- 第一版服务对象是有变美目标的普通用户。
- 第一版核心闭环是：轻量 onboarding、初版行动型形象报告、每日场景穿搭/发型建议、用户反馈、记忆更新。
- 产品重点是持续记忆和进化，不是一次性测评、完整衣橱库存系统或电商导购平台。

## MVP 边界

第一版必须优先支持：

- 对话式 onboarding。
- 自拍、半身照或核心衣橱照片上传。
- 用户画像、风格偏好、禁忌和场景需求沉淀。
- 初版行动型个人形象报告。
- 基于真实场景生成穿搭和发型建议。
- 对每条建议记录采纳、拒绝、修改和实际反馈。
- 根据反馈更新长期记忆。
- 人工精选明星风格库。
- 用户自选明星或上传参考图后的风格元素解析。
- 缺口单品建议，但不提供具体商品链接。

第一版不做：

- 完整电商导购或 SKU 推荐。
- 自动抓取公开明星图片。
- 完整衣橱库存管理。
- 减肥、医疗、植发、皮肤病、整形等高风险建议。
- 保证颜值提升的承诺。
- 未经授权公开复用用户上传图片或参考图。

## 明星风格库约束

- 明星风格库是风格参考层，不是相貌对比系统。
- 推荐表达应强调“参考造型逻辑”，例如比例、廓形、色彩、发型方向、单品结构和场景表达。
- 避免将核心结论写成“你像某明星”。
- 不自动从互联网抓取明星图片作为第一版能力。
- 人工维护的明星风格样本应记录可迁移元素和不可迁移风险。

## AI 与数据约束

- 不让大模型只依赖聊天上下文裸跑；关键用户信息应沉淀为结构化档案。
- 区分用户明确陈述的事实和 AI 推断的判断。
- AI 推断应带有不确定性表达，必要时记录置信度，并允许用户修正。
- 真实用户反馈优先级高于初始诊断和相似度匹配。
- 推荐结果应尽量具体、可执行，并说明为什么适合、哪些不建议做、有什么替代方案。

## 隐私与表达边界

- 自拍、身材、衣橱、审美偏好和反馈都视为敏感个人数据。
- 产品设计和实现必须保留用户删除照片、档案和反馈数据的能力。
- 避免羞辱式或制造焦虑的表达。
- 对身材、肤色、年龄感、脸型、发量等描述要中性、具体、可行动。
- 不输出医疗诊断、病理判断或需要专业资质的建议。

## 工程约束

- 在修改代码前先阅读现有结构，沿用项目已有技术栈、命名和目录约定。
- 不做与当前任务无关的重构。
- 不回滚用户或其他工具产生的未明确归属改动。
- 手工编辑文件优先使用 `apply_patch`。
- 搜索文件和文本优先使用 `rg`。
- 新增功能应配套必要的测试或可验证流程。
- 完成实现前必须运行相关验证命令；无法运行时要说明原因。

## Git 约束

- 提交前检查 `git status --short`。
- 不把无关未跟踪文件加入提交。
- 提交信息使用简洁中文或英文均可，但文档型提交优先中文。
- 不使用 `git reset --hard`、`git checkout --` 等破坏性命令，除非用户明确要求。

## 设计文档位置

- 产品设计和 spec 放在 `docs/superpowers/specs/`。
- 实现计划放在 `docs/superpowers/plans/`。
- 以后新增的产品/技术文档默认使用中文。
